Jinja2 就是高级的 .format() / f-string，相比f简单替换，还可以写逻辑。Jinja2将template解析成AST，带入data，输出字符串

Jinja 是模板语言本身（SQL），Jinja2 是 Python 中对它的实现（MySQL/PostgreSQL）

### Qwen3-4B-Thinking-2507 vs. Qwen3-14B

Qwen3-4B-Thinking-2507 chat template(partial):
```jinja
{%- if add_generation_prompt %}
    {{- '<|im_start|>assistant\n<think>\n' }}
{%- endif %}
```
`{%- if add_generation_prompt %}`: true or false, model whether to generate

`<think>` is forced to write in prompt, not generated by model, and model generation will not include `<think>`

Qwen3-14B chat template(partial):
```jinja
{%- if add_generation_prompt %}
    {{- '<|im_start|>assistant\n' }}
    {%- if enable_thinking is defined and enable_thinking is false %}
        {{- '<think>\n\n</think>\n\n' }}
    {%- endif %}
{%- endif %}
```
When thinking is disabled, prompt will include `'<think>\n\n</think>\n\n'` and model generation will not include `<think>`. Otherwise, model infers starting with `<think>`

### why var like `{{output}}` not in the jinja template
jinja template is to build prompt, not to fetch or concatenate model generation 
